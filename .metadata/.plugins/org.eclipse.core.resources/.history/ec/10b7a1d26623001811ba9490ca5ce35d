package fi.hh.palvelinohjelmointi.bookstore.bookstore;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import fi.hh.palvelinohjelmointi.bookstore.bookstore.webcontrol.BookController;
import fi.hh.palvelinohjelmointi.bookstore.bookstore.webcontrol.UserController;

@RunWith(SpringRunner.class)
@SpringBootTest
public class BookstoreApplicationTests {

	
	@Autowired
	private BookController bcontorller;
	
	@Autowired
	private UserController ucontoller;
	
	@Test
	public void contexLoads() throws Exception {
		assertThat(bcontorller).isNotNull();
	}

}
